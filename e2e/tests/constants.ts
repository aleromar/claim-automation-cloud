// Single source for the stub-IdP operator identity inside the e2e package —
// the backend env (playwright.config.ts) and every spec must agree on it.
export const OPERATOR = "operator@example.com";
